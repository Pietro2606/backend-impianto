{
  "name": "impianto-backend",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "generate": "npx prisma generate --schema=prisma/schema.prisma",
    "migrate": "npx prisma migrate dev --name init --schema=prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.12.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "prisma": "^5.12.0"
  }
}
